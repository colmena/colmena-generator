import { Component, Input } from '@angular/core'

@Component({
  selector: 'app-{{ moduleFileName }}-header',
  template: `
    <h5 class="my-2">
      <span *ngIf="item; else message">
        {{item.name}} <small>{{item.id}}</small>
      </span>
      <ng-template #message>Add New {{ moduleName }}</ng-template>
    </h5>
  `,
})
export class {{ moduleName }}HeaderComponent {

  @Input() item

}
