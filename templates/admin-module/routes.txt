import { Routes } from '@angular/router'

import { {{ properCase model }}ListComponent } from './containers/{{ dashCase model }}-list.component'
import { {{ properCase model }}DetailComponent } from './containers/{{ dashCase model }}-detail.component'
import { {{ properCase model }}FormComponent } from './components/{{ dashCase model }}-form.component'

import { {{ properCase model }}Resolver } from './{{ dashCase model }}s.resolvers'

export const {{ properCase model }}sRoutes: Routes = [
  {
    path: '{{ camelCase model }}s',
    data: { title: '{{ properCase model }}s' },
    children: [
      {
        path: '',
        redirectTo: 'list',
        pathMatch: 'full',
      },
      {
        path: 'list',
        component: {{ properCase model }}ListComponent,
        data: { title: 'List' },
      },
      {
        path: 'create',
        component: {{ properCase model }}DetailComponent,
        data: { title: 'Create' },
        children: [
          {
            path: '',
            component: {{ properCase model }}FormComponent,
          },
        ],
      },
      {
        path: ':id',
        component: {{ properCase model }}DetailComponent,
        resolve: {
          {{ camelCase model }}: {{ properCase model }}Resolver,
        },
        data: { title: '{{ camelCase model }}' },
        children: [
          {
            path: '',
            redirectTo: 'edit',
            pathMatch: 'full',
          },
          {
            path: 'edit',
            component: {{ properCase model }}FormComponent,
            data: { title: 'Edit' },
          },
        ],
      },
    ],
  },
]
