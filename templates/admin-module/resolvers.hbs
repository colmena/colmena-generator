import { Injectable } from '@angular/core'
import { Resolve, RouterStateSnapshot, ActivatedRouteSnapshot } from '@angular/router'
import { Observable } from 'rxjs/Observable'

import { {{ moduleName }}, {{ moduleNamePlural }}Service } from './{{ moduleFileName }}.service'

@Injectable()
export class {{ moduleNamePlural }}Resolver implements Resolve<{{ moduleName }}> {

  constructor(private service: {{ moduleNamePlural }}Service) {}

  resolve(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): Observable<{{ moduleName }}> {
    return this.service.getItem(route.params.id)
  }
}
