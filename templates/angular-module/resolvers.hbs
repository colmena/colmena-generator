import { Injectable } from '@angular/core'
import { Resolve, RouterStateSnapshot, ActivatedRouteSnapshot } from '@angular/router'
import { Observable } from 'rxjs/Observable'

import { {{ moduleNameProper }}, {{ moduleNameProperPlural }}Service } from './{{ moduleFileNamePlural }}.service'

@Injectable()
export class {{ moduleNameProperPlural }}Resolver implements Resolve<{{ moduleNameProper }}> {

  constructor(private service: {{ moduleNamePlural }}Service) {}

  resolve(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): Observable<{{ moduleNameProper }}> {
    return this.service.getItem(route.params.id)
  }
}
